[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd frontend && npm install && npm run build && cd .. && cp -r frontend/build/* backend/public/"

[deploy]
startCommand = "cd backend && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
